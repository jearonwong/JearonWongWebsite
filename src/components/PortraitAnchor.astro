---
// Portrait Anchor Component
import { siteConfig } from "../data/site";

const avatarWebp = "/images/jearon-wong-avatar.webp";
const avatarJpg = "/images/jearon-wong-avatar.jpg";
const avatarAlt = `${siteConfig.author.name} portrait`;
---

<div class="portrait-anchor">
  <div class="portrait-panel registry-chassis">
    <picture class="portrait-picture">
      <source srcset={avatarWebp} type="image/webp" />
      <img
        src={avatarJpg}
        alt={avatarAlt}
        class="portrait-image"
        width="1200"
        height="1200"
        loading="eager"
        decoding="async"
      />
    </picture>
    <div class="portrait-shade"></div>
    <div class="registry-grid portrait-grid"></div>
  </div>
</div>

<style>
  .portrait-anchor {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    width: 100%;
    height: 100%;
  }
  .portrait-panel {
    position: relative;
    width: 100%;
    max-width: 500px;
    aspect-ratio: 0.82 / 1;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    background:
      linear-gradient(145deg, rgba(59, 130, 246, 0.08), rgba(255, 255, 255, 0)),
      var(--current-surface);
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow:
      0 28px 70px rgba(0, 0, 0, 0.38),
      inset 0 0 0 1px rgba(255, 255, 255, 0.025);
  }
  .portrait-picture,
  .portrait-image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }
  .portrait-image {
    object-fit: cover;
    object-position: 50% 42%;
  }
  .portrait-shade {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg, rgba(17, 17, 17, 0) 52%, rgba(17, 17, 17, 0.28) 100%),
      linear-gradient(90deg, rgba(17, 17, 17, 0.1), rgba(17, 17, 17, 0) 48%, rgba(17, 17, 17, 0.18));
    pointer-events: none;
  }
  .portrait-grid {
    position: absolute;
    inset: 0;
    opacity: 0.06;
    pointer-events: none;
  }
  @media (max-width: 1024px) {
    .portrait-anchor {
      justify-content: flex-start;
    }

    .portrait-panel {
      max-width: 440px;
      aspect-ratio: 1.08 / 1;
    }
  }
  @media (max-width: 640px) {
    .portrait-panel {
      max-width: 100%;
      aspect-ratio: 1.25 / 1;
    }

    .portrait-image {
      object-position: 50% 38%;
    }
  }
</style>
