# Public-Facing Internal Term Scan Report

## Scan Terms

`WP1`, `WP2`, `WP3`, `WP1/WP2`, `WP1 and WP2`, `WP1 / WP2`, `R3AA`, `R3AF`, `R3AC`, `R8`, `route restored`, `artifact candidate`, `internal candidate`, `publication candidate source`.

## Repaired Public-Facing Hits

- WP3 public HTML: repaired 40 `WP1`, 45 `WP2`, 19 `WP3`, and one `R8` visible artifact hit.
- WP3 public manifest: repaired `WP3`, `R3AC`, `WP2`, `WP1/WP2`, and `R8` public text fields.
- WP3 route data: repaired public overview, evidence-integrity, and status panel rows.
- R3AE X short post draft: repaired `WP3 of the Agentic Lifecycle Governance Industry Series` to the formal public paper title.
- PDF metadata: regenerated with public-facing A4 print profile metadata rather than the internal rendering label.

## Acceptable Internal Governance Hits

- Source-code IDs such as `wp1-gaic`, `wp2-aiaawp`, and `wp3-aiirwp` remain in `src/data/whitepaperPublications.ts` and route loaders. These are implementation identifiers, not public copy.
- R3AF/R3AG package and governance records retain wave IDs because they are internal governance records.

## Prior-Paper Artifact-History Hits

The broad repository scan still finds pre-existing prior-paper artifact-history terminology in WP1/WP2 public maintenance records and WP2 artifact appendix text. These are not part of the WP3 R3AG resealed artifact set and were not introduced by this wave. They are recorded as a future cross-series public terminology hardening opportunity, not a blocker for the WP3 R3AG amendment.

## Unresolved WP3 Public Hits

None.
