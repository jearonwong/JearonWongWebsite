# Integration Table and Layout Risk Plan

## Purpose

This plan carries forward table and layout risks for a future internal integrated draft. R3O does not implement layout and does not generate artifacts.

## Body Table Preservation

R3P must preserve body table IDs:

- `T-02-01`.
- `T-04-01` through `T-27-01`.

R3P must not create duplicate body table IDs or convert appendix tables into body table IDs.

## Appendix H Placement

Appendix H should remain after Appendix G as the table inventory and layout risk register. It should remain planning/reference material only.

Appendix H should:

- inventory the body table IDs;
- flag wide-table risks;
- identify future layout-treatment candidates;
- avoid implementing HTML, PDF, CSS, DOCX, manifest, checksum, public route, or artifact work.

## Wide-Table Risk List

The following body tables remain future layout risks:

- `T-02-01`.
- `T-09-01`.
- `T-14-01`.
- `T-18-01`.
- `T-21-01`.
- `T-27-01`.

## Future Treatment Candidates

Potential future artifact-planning treatments, not authorized in R3O or R3P unless later scoped:

- responsive HTML wrappers;
- PDF landscape treatment;
- abbreviated body table with appendix detail;
- appendix-summary parity QA;
- table footnote compression;
- body-to-appendix table consistency checks;
- line-break and column-width tuning.

## Appendix Summary Candidates

The following may need later body/appendix split or summary treatment:

- `T-02-01`.
- `T-09-01`.
- `T-14-01`.
- `T-18-01`.
- `T-21-01`.
- `T-27-01`.

This is not permission to revise the body in R3O. R3P may only integrate according to the accepted plan.

## Artifact Boundary

R3O creates no:

- HTML;
- PDF;
- DOCX;
- CSS/layout implementation;
- manifest;
- checksum;
- public artifact;
- public route update;
- publication-readiness claim.

## R3P Recommendation

Carry forward wide-table risks and preserve Appendix H as an internal layout-risk register in the integrated draft.
