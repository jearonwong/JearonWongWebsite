# Candidate Source Path and Naming Plan

## Purpose

Plan the future path and naming convention without creating the file.

## Placeholder Path Proposal

Future placeholder path, if a later wave explicitly authorizes candidate source creation:

`gaic_v032_work/source_r3/whitepapers/aiirwp/v1/artifacts/r3x-candidate-source-preparation/AIIRWP-v1.0-publication-candidate-source.md`

R3V does not create this path or file.

## Naming Rules

A future candidate source filename should:

- include `AIIRWP`;
- include `v1.0`;
- include `publication-candidate-source` only if a later wave explicitly authorizes candidate source creation;
- avoid `final`, `sealed`, `release-ready`, `public`, or `published` unless separately authorized;
- preserve internal/public status distinction;
- keep source outside `public/research` until a later public artifact wave.

## Path Rules

- The candidate source should remain inside the controlled `gaic_v032_work/source_r3/whitepapers/aiirwp/v1/` workspace until a later public artifact wave.
- It should not be created under `public/research`.
- It should not be used to restore public route, CTA, staging, sitemap, robots, `llms.txt`, entity graph, or release package status by itself.

## R3V Boundary

R3V defines placeholder path and naming rules only. It does not create directories, candidate source files, public files, or generated artifacts.
